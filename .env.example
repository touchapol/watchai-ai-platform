# Database (PostgreSQL with connection pool settings)
DATABASE_URL="postgresql://username:password@localhost:5432/watchai?connection_limit=5&pool_timeout=30&connect_timeout=30"

# MongoDB (for Messages and Logs)
MONGODB_URI="mongodb://username:password@localhost:27017/watchai?authSource=admin"

# Password Encryption Secret (32 bytes for AES-256)
PASSWORD_SECRET_KEY="your-32-character-secret-key-here"

# JWT Secret (for authentication tokens)
JWT_SECRET="your-jwt-secret-key-here"

# API Key Encryption Secret (for encrypting stored API keys)
API_KEY_SECRET="your-api-key-encryption-secret!!"
