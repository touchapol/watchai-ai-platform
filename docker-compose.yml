name: watchai

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
